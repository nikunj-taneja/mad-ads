# Meta credential checker

This read-only tool confirms that the configured token can read the configured ad account.

```bash
python tools/meta-ads-auditor/audit.py
python tools/meta-ads-auditor/audit.py --list-campaigns
```

It performs GET requests only. See `../meta-ads-uploader/README.md` for setup.
