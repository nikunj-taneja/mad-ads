# Security

Do not report a real credential in a public issue. Revoke it first, then use the repository host's private security-reporting channel.

This template ignores common private inputs, but ignore rules are not a security boundary. Review staged files, enable secret scanning, and run `tools/privacy-scan.py` with client-specific patterns before publishing.
